// cannot be re-declared but can be updated later and declared as a local scoped varibale
let my_name = "Ayush"

// can be re-declared and declared as a global variable
var my_class = "10th"

// cannot be re-declared or updated and declared as a local varibale
const DOB = "08/09/2010"

console.log(`Name : ${my_name}\nClass : ${my_class}\nDOB : ${DOB}`)